{
  "name": "paradise-roleplay-pk",
  "version": "1.0.0",
  "description": "Official website for Paradise Roleplay Pakistan",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy-files",
    "copy-files": "cp src/.htaccess dist/ && cp src/_redirects dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^4.4.9"
  },
  "devDependencies": {
    "sass": "^1.58.0"
  }
}

